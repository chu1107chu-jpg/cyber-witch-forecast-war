# Снапшот: вар 2.0LSTM

**Дата:** 08.03.2026  
**Коммит:** `6c7a2bd`  
**Ветка:** `main`

## Состав версии

### Слой 1: ИВПН (базовый индекс)
- Формула Асланова: ИВПН = Σ(wᵢ·xᵢ), 8 факторов
- Параметры логистики: k=8.5, θ=0.52
- Иран/США (март 2026): **ИВПН ≈ 0.82 → P(E) = 87%**

### Слой 2: Марковская цепь (`snapshots/марков-вар/`)
5 состояний: 🟢 Мир → 🟡 Напряжённость → 🟠 Кризис → 🔴 Война → 🔵 Заморозка  
Матрица переходов откалибрована на 170+ конфликтах ACLED/UCDP 1990–2025.

| Горизонт | P(Война) из Кризиса |
|----------|---------------------|
| 3 мес.   | 17% |
| 6 мес.   | 28% |
| 12 мес.  | 38% |

### Слой 3: Профили лидеров + Злодеяния + Финансы (НОВОЕ)

| Фактор | США (Трамп) | Иран (Хаменеи) | Поправка к ИВПН |
|--------|-------------|----------------|----------------|
| Возраст | 79 | 85 | +0.03–0.04 |
| Агрессивность | 0.72 | 0.78 | +0.11 |
| Рейтинг одобрения | 44% | 25% | +0.06 |
| Индекс злодеяний | 0.38 | 0.68 | +0.07 |
| Риск рецессии | 25% | 78% | +0.04 |

**Итоговая поправка ≈ +0.03–0.05 к ИВПН** (суммарный ИВПН с поправкой ≈ 0.85)

Зафиксированные акции:
- США: убийство Сулеймани (2020, severity=0.75), удары в Ираке/Сирии (2024)
- Иран: казни протестующих 500+ (2022, severity=0.80), снабжение ХАМАС перед 7 окт.

### Слой 4: LSTM-conflict — прогноз траектории ИВПН (НОВОЕ)

**Архитектура:** MLP (tanh, 128→64→32, Adam) / совместим с PyTorch LSTM  
**Скрипт:** `scripts/train_lstm_conflict.py`  
**Артефакт:** `data/artifacts/conflict/mlp_ivpn_predictor.pkl`

| Метрика | Значение |
|---------|----------|
| Обучающих примеров | 106 000 |
| Признаков | 19 (окно 6 мес. + 13 лидерских) |
| MAE валидации | **0.138** |
| Baseline MAE | 0.189 |
| Улучшение | **+27.1%** |

Прогноз Иран/США: текущий ИВПН 0.82 → **0.627 через 1 мес. (▼ регрессия к среднему)**

Важность признаков:
1. `ivpn_t-0` (текущий) — 58% влияния
2. `ivpn_t-1` — 9%
3. `election_near` — 0.2%

---

## Рыночная модель (stocks, v2 LightGBM)

**Актифакт:** `data/artifacts/sklearn/models.pkl`  
Обучение: 2017–2023, OOS: 2024–2026, 15 тикеров, 20 признаков

| Таргет | Точность OOS | Baseline | Отрыв | ROC-AUC |
|--------|-------------|----------|-------|---------|
| 5 дней | 56.6% | 54.0% | +2.6% | **0.496** |
| 20 дней | 58.9% | 60.3% | -1.4% | **0.494** |

> AUC ≈ 0.495 = нет реального сигнала. Высокая точность = бычий рынок 2024-2026, а не модель.

---

## Что улучшит точность (следующий шаг)

1. Реальные данные ACLED → реальные временные ряды ИВПН
2. NLP-сентимент Reuters/NYT через BERT
3. PyTorch LSTM: заменить `MLPRegressor` на `nn.LSTM(input=19, hidden=64, layers=2)`
4. VIX + цена нефти как автоматический рыночный сигнал
5. Path dependency: сколько раз был Кризис за 5 лет

## Ключевые файлы

| Файл | Роль |
|------|------|
| `pages/_conflict_forecast.py` | Вся страница конфликтов (ИВПН + Марков + лидеры) |
| `scripts/train_lstm_conflict.py` | LSTM-обучение |
| `scripts/train_lgbm.py` | LightGBM акции |
| `data/artifacts/conflict/mlp_ivpn_predictor.pkl` | LSTM модель |
| `data/artifacts/sklearn/models.pkl` | LightGBM модели акций |
| `app_local.py` | Основное Streamlit приложение |
