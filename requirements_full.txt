# ── Core ML ──────────────────────────────────────────────────
pandas>=2.1
numpy>=1.26
scikit-learn>=1.4
torch>=2.2
torchvision>=0.17
tensorflow>=2.16
opencv-python-headless>=4.9
ultralytics>=8.1

# ── API ──────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.7
python-dotenv>=1.0

# ── ETL / HTTP ───────────────────────────────────────────────
requests>=2.31
feedparser>=6.0
beautifulsoup4>=4.12
lxml>=5.1
tqdm>=4.66
yfinance>=0.2
pycoingecko>=3.1

# ── NLP ──────────────────────────────────────────────────────
transformers>=4.40
sentence-transformers>=2.7
langdetect>=1.0.9

# ── Storage / DB ─────────────────────────────────────────────
supabase>=2.4
boto3>=1.34          # Cloudflare R2 (S3-совместимое)

# ── Config / Utils ───────────────────────────────────────────
pyyaml>=6.0
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7

# ── Frontend ─────────────────────────────────────────────────
streamlit>=1.35
plotly>=5.21

# ── Crypto adapters (stubs) ──────────────────────────────────
# tonsdk  # раскомментировать при включении TON
# solana  # раскомментировать при включении Solana
